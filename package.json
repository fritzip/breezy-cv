{
  "name": "breezy-cv",
  "version": "1.7.0",
  "description": "Static CV generator with PDF support",
  "main": "build.js",
  "bin": {
    "breezy-cv": "./build.js"
  },
  "scripts": {
    "build": "npx breezy-cv build",
    "dev": "npx breezy-cv build && (npm run watch & npm run serve)",
    "watch": "nodemon -e yaml,json,css,ejs,js --watch resume.yaml --watch config.yaml --watch themes --exec \"npm run build\"",
    "serve": "npx serve public"
  },
  "dependencies": {
    "ejs": "^3.1.9",
    "js-yaml": "^4.1.0",
    "nodemon": "^3.1.11",
    "serve": "^14.2.1"
  },
  "devDependencies": {}
}
